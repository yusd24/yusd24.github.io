{% comment %}
  Unified conferences/workshops organized entry renderer using the "Publications Method" (Margin-Left)
{% endcomment %}

<ul class="card-text font-weight-light list-group list-group-flush">
  {% for entry in entries %}
    <li class="list-group-item">
      <div class="row">
        
        {% comment %} Date Calculation (Kept your range logic!) {% endcomment %}
        {% assign raw_start = entry.start_date | default: entry.startDate %}
        {% assign raw_end = entry.end_date | default: entry.endDate %}

        {% if raw_start %}
          {% assign start_pieces = raw_start | strip | split: '-' %}
          {% assign startDate = start_pieces[1] | append: '/' | append: start_pieces[0] %}

          {% if raw_end %}
            {% assign end_pieces = raw_end | strip | split: '-' %}
            {% assign endDate = end_pieces[1] | append: '/' | append: end_pieces[0] %}
          {% else %}
            {% assign endDate = 'Present' %}
          {% endif %}
          {% assign date = startDate | append: ' - ' | append: endDate %}
        {% else %}
          {% assign date = null %}
        {% endif %}

        {% comment %} Left Column: Bulletproof Centering without Tables {% endcomment %}
        <div class="col-xs-2 col-sm-2 col-md-2 text-center date-column">
          
          {% if date %}
            <div class="mb-2">
              <span class="badge font-weight-bold danger-color-dark text-uppercase align-middle" style="min-width: 75px">
                {{ date }}
              </span>
            </div>
          {% endif %}
          
          {% comment %} Location with fixed capture bug and smaller font {% endcomment %}
          {% if entry.location %}
            <div>
              <p class="location mb-0" style="font-size: 0.75rem; line-height: 1.2;">
                <i class="fa-solid fa-location-dot iconlocation"></i>
                {{ entry.location }}
              </p>
            </div>
          {% endif %}
          
        </div>
        
        {% comment %} Right Column: The Text Content {% endcomment %}
        <div class="col-xs-10 col-sm-10 col-md-10 mt-2 mt-md-0">
          
          {% comment %} Title (Separated from the Link) {% endcomment %}
          <h6 class="title font-weight-bold ml-1 ml-md-4">{{ entry.name }}</h6>
          
          {% comment %} Authors with Margin-Left {% endcomment %}
          {%- if entry.authors -%}
            <h6 class="ml-1 ml-md-4" style="font-size: 0.85rem;">
              {{ entry.authors | array_to_sentence_string }}
            </h6>
          {%- endif -%}
          
          {% comment %} Summary with Margin-Left {% endcomment %}
          {% if entry.summary %}
            <p class="ml-1 ml-md-4" style="font-size: 0.85rem;">
              {{ entry.summary | markdownify | remove: '<p>' | remove: '</p>' }}
            </p>
          {% endif %}

          {% comment %} Highlights with Margin-Left {% endcomment %}
          {% if entry.highlights %}
            <ul class="items ml-1 ml-md-4">
              {% for item in entry.highlights %}
                <li>
                  <span class="item" style="font-size: 0.85rem;">
                    {{ item | markdownify | remove: '<p>' | remove: '</p>' }}
                  </span>
                </li>
              {% endfor %}
            </ul>
          {% endif %}

          {% comment %} NEW: Link displayed at the bottom {% endcomment %}
          {% if entry.url %}
            <div class="ml-1 ml-md-4 mt-2" style="font-size: 0.85rem;">
              <a href="{{ entry.url }}" target="_blank">
                <i class="fa-solid fa-link"></i> {{ entry.url }}
              </a>
            </div>
          {% endif %}
          
        </div>
      </div>
    </li>
  {% endfor %}
</ul>
